name: Release

on:
  pull_request:
    branches: [main]
    types: [closed]
  workflow_dispatch:
    inputs:
      release_type:
        description: 'release type (major, minor, patch)'
        required: true
        default: 'patch'

jobs:
  create-release:
    if: >
      ${{
        github.event_name == 'workflow_dispatch' ||
        (
          github.event.pull_request.merged == true &&
          (
            contains(github.event.pull_request.labels.*.name, 'major') ||
            contains(github.event.pull_request.labels.*.name, 'minor') ||
            contains(github.event.pull_request.labels.*.name, 'patch')
          )
        )
      }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      tag: ${{ steps.semver.outputs.tag }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Determine release type
        id: rtype
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            LABEL=${{ toJSON(github.event.pull_request.labels) }}
            # Look for one of the semantic labels
            if echo "$LABEL" | grep -q '"name":"major"'; then echo "type=major" >> "$GITHUB_OUTPUT"; fi
            if echo "$LABEL" | grep -q '"name":"minor"'; then echo "type=minor" >> "$GITHUB_OUTPUT"; fi
            if echo "$LABEL" | grep -q '"name":"patch"'; then echo "type=patch" >> "$GITHUB_OUTPUT"; fi
          else
            echo "type=${{ github.event.inputs.release_type }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Compute next semver tag
        id: semver
        run: |
          git fetch --tags
          LATEST=$(git describe --tags --abbrev=0 2>/dev/null || echo "0.0.0")
          echo "Latest tag: $LATEST"
          IFS='.' read -r MAJ MIN PAT <<<"$LATEST"
          TYPE=${{ steps.rtype.outputs.type }}
          if [ "$TYPE" = "major" ]; then MAJ=$((MAJ+1)); MIN=0; PAT=0; fi
          if [ "$TYPE" = "minor" ]; then MIN=$((MIN+1)); PAT=0; fi
          if [ "$TYPE" = "patch" ]; then PAT=$((PAT+1)); fi
          NEW_TAG="$MAJ.$MIN.$PAT"
          echo "New tag: $NEW_TAG"
          echo "tag=$NEW_TAG" >> $GITHUB_OUTPUT

      - name: Generate changelog
        id: changelog
        run: |
          git fetch --tags
          LATEST=$(git describe --tags --abbrev=0 2>/dev/null || echo "0.0.0")
          NEW_TAG=${{ steps.semver.outputs.tag }}
          if [ "$LATEST" = "0.0.0" ]; then
            CHANGELOG=$(git log --pretty=format:'- %s (%h)' -n 50)
          else
            CHANGELOG=$(git log --pretty=format:'- %s (%h)' $LATEST..HEAD)
          fi
          if [ -z "$CHANGELOG" ]; then
            CHANGELOG="No notable changes"
          fi
          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGELOG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create or update GitHub release
        run: |
          TAG="${{ steps.semver.outputs.tag }}"
          NOTES="${{ steps.changelog.outputs.changelog }}"

          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release edit "$TAG" --title "$TAG" --notes "$NOTES"
          else
            gh release create "$TAG" --title "$TAG" --notes "$NOTES" --target "$GITHUB_SHA"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  build-and-push:
    needs: create-release
    permissions:
      contents: read
      packages: write
      id-token: write
    uses: ./.github/workflows/docker-build-and-push.yml
    with:
      version_tag: ${{ needs.create-release.outputs.tag }}
      push_latest: true
